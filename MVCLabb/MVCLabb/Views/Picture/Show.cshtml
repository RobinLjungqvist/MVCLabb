@model IEnumerable<MVCLabb.Models.PictureViewModel>

<h2>@ViewBag.GalleryName</h2>
<ul>
    <li> @Html.ActionLink("Back to List", "Index", "Gallery") </li>
</ul>
@if (Model.Count() != 0)
    {
        
        foreach (var picture in Model)
        {
            <a href='@Url.Action("Details", "Picture", picture)'>
                <img src="@Url.Content($"{picture.Path}")" alt="@picture.Description" style="width: 300px" />
            </a>
    }
}
else
{
    <h2>Empty Gallery!</h2>
}
<p>
    @Html.ActionLink("Back to List", "Index", "Gallery")
</p>